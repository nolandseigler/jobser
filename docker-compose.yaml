version: '3.1'

services:

  db:
    image: postgres:16
    restart: always
    environment:
      POSTGRES_PASSWORD: postgres
  web:
    depends_on:
      - db
    build:
      context: ./jobserweb
      dockerfile: Dockerfile
    image: jobserweb:compose
    ports:
    - "127.0.0.1:8989:8080"
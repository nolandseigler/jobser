package auth

// newSessionHash -> <user_id>:<jti>

// NewJWT -> includes using redis storage to BustAllSessions

// ValidateJWT -> includes using redis storage to check session

// BustSessions -> invalidates all user sessions in redis storage
